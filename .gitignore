node_modules/
dist/
build/
# The web build, copied in when the api serves it the way the container does.
apps/api/public/
# What the build's prerender pass compiles for Node, then throws away.
dist-ssr/
coverage/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example

# Your own copy of compose.example.yaml, holding your own passwords.
compose.yaml

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

playwright-report/
test-results/
blob-report/
.playwright/

# What vitest browser mode leaves behind when a test fails.
.vitest-attachments/
__screenshots__/

.vite/
.tanstack/
