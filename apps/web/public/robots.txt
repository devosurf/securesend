# A secret's address is not for an index. The instance says so in a header on
# every /s/ response and in the document itself; this is the third place, for a
# crawler that reads only this file.
User-agent: *
Disallow: /s/

# The pages that are meant to be found. The address is filled in by the
# instance serving this, because a Sitemap line has to be absolute and the build
# does not know what this instance is called.
Sitemap: %ORIGIN%/sitemap.xml
